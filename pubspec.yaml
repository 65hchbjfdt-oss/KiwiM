name: kiwimusic
description: KiwiMusic — бесконечный поток с ИИ
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  glassmorphism: ^3.0.0
  flutter_blurhash: ^1.3.0
  just_audio: ^0.9.48
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.0
  revenuecat_purchases_flutter: ^8.0.0
  encrypt: ^5.0.3
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/kiwi_logo.png  # потом добавишь логотип